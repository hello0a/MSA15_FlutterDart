name: path_provider_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  dev: ^1.0.0
  easy_radio: ^0.0.3
  flutter:
    sdk: flutter
  icon_checkbox: ^1.0.1
  mask_text_input_formatter: ^2.9.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

assets:
  - repo/test.txt
